name: gemini_chatbot
description: A simple AI chatbot using the Gemini API.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  logger: ^2.0.1
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.6
  cloud_firestore: ^4.15.3
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:

